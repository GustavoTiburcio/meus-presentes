import styled from 'styled-components';

export const Input = styled.select`
  padding: 10px;
  background: #fafafa;
  border: none;

  font-weight: 500;

  :nth-child(2n) {
    margin: 5px 0px
  }
`;
